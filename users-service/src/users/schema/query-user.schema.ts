// import PaginationParams from '@shared/schema/pagination-params.schema';
import QueryParamsSchema from '@common/schema/query-params.schema';

const QueryUserSchema = QueryParamsSchema.extend({

});

export default QueryUserSchema;
