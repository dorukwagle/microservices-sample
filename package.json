{
  "name": "my-app",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix api-gateway\" \"npm run start:dev --prefix users-service\" \"npm run start:dev --prefix auth-service\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
